# Squire AI - locked dependency versions (see docs/version-matrix/mc-1.20.1.md)
# All versions are pinned; floating versions are forbidden by the spec (Section 3).

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Minecraft / Fabric stack
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.19.3
fabric_version=0.92.11+1.20.1

# Build tooling
loom_version=1.11.8

# Mod metadata
mod_version=0.1.0
maven_group=dev.squire
archives_base_name=squire

# Pure-Java test dependencies (not bundled into the mod jar)
junit_bom_version=5.10.2
